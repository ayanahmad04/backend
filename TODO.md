# TODO - Undo commit & open PR from fork

- [ ] Create a new branch for the commit to be PR’d (optional but recommended)
- [ ] Push branch to fork remote (`origin`)
- [ ] Open PR on GitHub: base = `upstream:main`, compare = `origin:<branch>`
- [ ] If the commit was already pushed to `upstream`, decide between revert or force-push (based on user preference)

